## [2026-07-06 23:52] 섹터별 자금 수급 주체 및 포함 종목 표 개선
- **작업 목적:** 섹터별 거래대금 순위를 유지하면서 외국인·기관의 수급 방향과 실제 계산 대상 종목을 한눈에 확인할 수 있도록 표를 개선했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 섹터별 외국인·기관·합산 순매수를 각각 억 원 단위로 표시
  - 외국인과 기관의 부호에 따라 동반 순매수·동반 순매도·외인 매수 우위·기관 매수 우위로 분류
  - 거래대금 TOP60에서 섹터별 계산에 포함된 종목 수와 종목명 유지
  - 기존과 동일하게 섹터 합산 거래대금 내림차순으로 정렬
---

## [2026-07-06 23:46] 시장강도 표 비차익 수급 변화 표시
- **작업 목적:** 베이시스와 해석이 겹치는 차익 순매수 열을 정리하고 실제 현물 바스켓 수급인 비차익 순매수의 변화 방향을 쉽게 확인하도록 개선했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 시간별 시장강도 표에서 `차익 순매수` 열 제거
  - 기존 변화 방향의 프로그램 개선·약화 표시를 비차익 개선·약화로 변경
  - 비차익 수급의 부호가 바뀌면 `순매수 전환` 또는 `순매도 전환`으로 표시
---

## [2026-07-06 23:33] 종가베팅 무적차트 스캐너 및 전용 탭 추가
- **작업 목적:** KIS 일봉 지표와 당일 시세를 결합해 종가베팅 후보를 자동 선별하고 웹페이지에서 날짜·시간별 결과를 확인할 수 있도록 추가했습니다.
- **영향을 받은 파일:** `close_bet_scanner.py`, `main.py`, `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 전체 시장 시가총액 조회를 우선 사용하고 조회 불가 시 당일 수집 DB로 대체해 5,000억 원 이상 종목 구성
  - RSI·Williams %R·MACD·일목균형표·OBV·거래량 비율 계산
  - 기존 조건에 따라 S급 최적타점·A급 매복·S급 과열주의 후보 분류
  - KIS 일봉에 당일 현재가·고가·저가·시가·누적 거래량을 반영해 장중 스캔 정확도 보완
  - `close_bet_scans`와 실행 이력 테이블에 날짜·분석시간별 결과, 검사·포착·실패 종목 수 저장
  - 전체 자동·수동 분석에 스캐너를 연결하고 오류 발생 시 기존 주 분석은 계속 실행
  - 웹페이지에 `종가베팅 스캐너` 탭을 추가해 선택 날짜·시간별 결과와 스캔 완료 시각 표시
---

## [2026-07-03 03:30] 종목별 중복·유사 뉴스 및 점수 중복 제거
- **작업 목적:** 동일 뉴스가 여러 언론사에서 재배포되거나 링크·제목이 조금씩 달라 뉴스 목록과 종목 점수가 중복 반영되는 문제를 해결했습니다.
- **영향을 받은 파일:** `news_collector.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 제목 끝 언론사명·특수문자·공백·속보 표기를 제거한 정규화 제목 생성
  - 같은 링크 또는 정규화 제목 유사도 88% 이상인 기사는 종목별 최초 한 건만 저장
  - 긍정·부정 판정이 서로 다른 기사는 의미 충돌을 막기 위해 유사해도 별도 유지
  - 중복 제거된 기사만 감성 점수를 한 번 반영하고 제거 후 최대 3건까지 저장
---

## [2026-07-03 03:15] 당일 뉴스 필터 및 주가 이벤트 가중 점수 적용
- **작업 목적:** 며칠 전 뉴스가 오늘 이슈로 섞이는 문제를 막고 수주·공급계약처럼 주가 영향이 큰 확정 이벤트를 일반 긍정 단어보다 중요하게 평가하도록 개선했습니다.
- **영향을 받은 파일:** `news_collector.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - Google 뉴스 검색에 최근 1일 조건을 추가하고 발행시각을 KST로 변환해 분석 대상 날짜와 같은 기사만 저장
  - 발행시각이 없거나 파싱되지 않는 기사 및 과거 기사 제외
  - 수주·공급계약·낙찰·기술수출은 +4, 최대 실적·흑자전환·승인은 +3 등 이벤트 중요도별 가중치 적용
  - 계약 해지·상장폐지·횡령·거래정지는 -4, 적자전환·어닝쇼크·임상 실패는 -3 등 부정 가중치 적용
  - 강한 부정 이벤트가 있으면 긍정 단어 점수를 무시하고 최종 점수를 -5~+5로 제한
  - 오늘 기사 확보를 위해 검색 후보는 확대하되 종목당 저장 건수는 기존 최대 3건 유지
---

## [2026-07-03 02:55] 장중 5회 분석 및 오전·오후·종가 시장강도 연결
- **작업 목적:** 웹 조회 시간을 09:30·09:50·11:30·14:00·16:00으로 확대하고 선택 시간에 맞는 시장강도 결과를 표시하며 16시에는 하루 전체 시장강도를 비교할 수 있도록 개선했습니다.
- **영향을 받은 파일:** `program_ws_collector.py`, `market_strength.py`, `crawler.py`, `main.py`, `cloud_job.py`, `app.py`, `deploy/oracle-cloud/afternoon-program.timer`, `deploy/oracle-cloud/afternoon-strength.timer`, `deploy/oracle-cloud/stock-analysis.timer`, `deploy/oracle-cloud/setup.sh`, `deploy/oracle-cloud/README.md`, `ai_changelog.md`
- **주요 변경 사항:**
  - TOP60 전체 분석을 09:30·09:50·11:30·14:00·16:00에 자동 실행하고 각 시각을 독립 세션으로 저장
  - 09:50 전체 분석에서 09:15·09:30·09:45 오전 시장강도를 함께 계산하고 기존 중복 09:50 전용 타이머 비활성화
  - 13:25 WebSocket 연결 후 13:30·13:45·14:00 프로그램 수급을 저장하고 14:01 오후 시장강도 계산
  - 15:40 종가 시장강도는 기존 14:30·15:00·15:20·15:30 흐름을 유지하며 16시 전체 분석에서 재계산하지 않음
  - 시장강도 탭은 선택 세션에 따라 오전·오후·종가 그룹을 자동 선택하고 16시에는 세 그룹 최종 점수 요약 표시
---

## [2026-07-03 02:20] 2시간 분봉 기반 섹터 순환매 분석 구현
- **작업 목적:** 대형 섹터의 절대 거래대금 착시를 제거하고 당일 2시간 단위로 가격 강도와 실제 방향성 거래대금 이동을 비교할 수 있도록 구현했습니다.
- **영향을 받은 파일:** `sector_flow_collector.py`, `crawler.py`, `cloud_job.py`, `app.py`, `requirements.txt`, `.gitignore`, `deploy/oracle-cloud/sector-flow.timer`, `deploy/oracle-cloud/setup.sh`, `deploy/oracle-cloud/README.md`, `ai_changelog.md`
- **주요 변경 사항:**
  - KIS 당일 분봉 API로 09:00~11:00·11:00~13:00·13:00~15:00·15:00~15:30 구간의 TOP60 대표 종목 수집
  - 양봉 거래대금은 양수, 음봉 거래대금은 음수로 계산해 섹터별 순유입 거래대금·총 거래대금·구간 수익률·상승 종목 수 저장
  - 섹터 자체 당일 최저·최고 순유입을 Min-Max 0~1로 정규화하고 부호 보존 상대 강도도 함께 저장
  - 30분 마감 구간과 120분 구간의 길이 차이는 분당 순유입·분당 거래대금으로 환산해 비교 왜곡 방지
  - Plotly 정규화 히트맵과 구간 수익률·순유입 증가 Z-Score 사분면 산점도 추가
  - Oracle 타이머를 11:05·13:05·15:05·15:35에 추가하고 기존 클라우드 작업의 DB 직렬화·자동 푸시 흐름에 연결
  - KIS 접근토큰을 만료 전까지 권한 600 로컬 캐시로 재사용해 불필요한 반복 발급 감소
  - 분봉 집계 데이터가 쌓이기 전에는 기존 스냅샷 기반 오늘 섹터 흐름 화면을 유지
---

## [2026-07-03 01:45] 오늘 섹터 흐름을 상승 강도와 거래대금 비중 선 그래프로 개편
- **작업 목적:** 히트맵이 직관적이지 않고 단순 거래대금만으로 섹터 순환매를 파악하기 어려워 상승률 강화·약화와 거래대금 이동을 함께 볼 수 있도록 개선했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 기존 자금 흐름 강도 히트맵 제거
  - 섹터 종목의 거래대금 가중 평균 등락률을 시간순 선 그래프로 표시하고 0% 기준선 추가
  - 같은 섹터의 구간 신규 거래대금 점유율을 별도 선 그래프로 표시해 가격 강도와 자금 비중을 비교
  - 구간별 상승 강도가 가장 커진 섹터와 가장 약해진 섹터, 해당 신규 거래대금 비중을 순환매 요약 표로 표시
  - 최신 스냅샷의 섹터 상승 강도·상승 종목 비율·종목 수 표 추가
  - 추적 섹터 개수는 기존 트렌드 분석 설정값을 사용
---

## [2026-07-03 01:25] 오늘 섹터 자금 이동 실험 탭 추가
- **작업 목적:** 장 초반 로봇에서 이후 반도체로 이동하는 것처럼 하루 중 시간대별 주도 섹터와 거래대금 이동을 시험적으로 확인할 수 있도록 추가했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 같은 날 연속 TOP60 스냅샷의 공통 종목을 기준으로 직전 대비 신규 거래대금 계산
  - 구간 거래대금 점유율과 섹터 내 상승 종목 비율을 결합한 자금 흐름 강도 히트맵 추가
  - 시간 구간별 1위 섹터 이동 요약과 상위 3개 업종의 거래대금·점유율·상승 비율·대표 종목 표 추가
  - 신규 진입 종목의 누적 거래대금이 구간 유입액으로 과대 계산되지 않도록 공통 종목만 비교
  - 현재 스냅샷이 2개 미만이면 데이터 부족 안내를 표시하며 장중 수집기는 아직 추가하지 않음
---

## [2026-07-03 01:05] 섹터 흐름 두 영역의 추적 개수와 최신 순위 정렬 통일
- **작업 목적:** 최근 섹터 흐름에서 설정한 섹터 수보다 적게 보이거나 최신 순위가 1위 다음 3위처럼 점프하고, 상승 업종 표가 설정 개수보다 많이 표시되는 문제를 해결했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 전체 섹터 흐름의 표시 대상을 주간 누적 상위에서 최신 정규장 거래대금 1위부터 N위까지로 변경
  - 최신일 상위 N개 업종의 최근 실제 전체 순위 흐름을 추적해 최신 시점 순위가 연속으로 표시되도록 구성
  - 전체 섹터와 상승 종목 주도 업종의 하단 표를 모두 사용자가 설정한 추적 섹터 수 N개로 제한
  - 상승 업종 선 그래프도 기존 최신일 상위 N개 기준을 유지해 설정값과 표시 선 개수를 통일
---

## [2026-07-03 00:50] 상승 업종 흐름 최신일 기준 및 전체 순위 표 수정
- **작업 목적:** 오늘 상승 업종이 여러 개인데 이번 주 누적 상위 업종 필터 때문에 일부만 표시되고 순위가 중간부터 보이는 혼란을 해결했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 선 그래프 표시 대상을 이번 주 누적 기준에서 최신 정규장 상승 거래대금 상위 N개 업종으로 변경
  - 최신일 주도 업종들의 최근 며칠 실제 전체 업종 순위 흐름을 추적
  - 하단 표는 최신일에 상승 종목이 있는 모든 업종을 누락 없이 실제 순위대로 표시
  - 기존 전체 섹터 거래대금 순위 영역은 변경하지 않음
---

## [2026-07-03 00:35] 상승 종목 주도 업종 거래대금 흐름 추가
- **작업 목적:** 거래대금 TOP60 중 상승한 종목만 업종별로 묶어 최근 며칠간 어느 업종에 상승 거래대금이 집중됐는지 확인할 수 있도록 추가했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 이번 주 정규장 TOP60에서 등락률이 0% 초과인 종목만 날짜별·업종별 거래대금 합산
  - 누적 상승 거래대금 상위 N개 업종의 일별 거래대금 순위를 1위가 위쪽인 선 그래프로 표시
  - 최신 날짜 기준 업종 순위·상승 거래대금·상승 종목 수·종목별 등락률 상세 표 추가
  - 상세 표는 모바일 화면에서 종목명이 가려지지 않도록 자동 줄바꿈 적용
  - 기존 전체 섹터 거래대금 순위 그래프와 표는 변경하지 않음
---

## [2026-07-03 00:20] 전기장비 통합 및 기타 종목 업종 보정
- **작업 목적:** `전기장비`와 `전력기기·전선`이 분리되고 주요 종목이 `기타`로 표시되는 문제를 사용자 지정 주식 섹터 기준으로 정리했습니다.
- **영향을 받은 파일:** `crawler.py`, `stock_data.db`, `ai_changelog.md`
- **주요 변경 사항:**
  - `전기장비`·`전력/전기장비`·`전기유틸리티`·`전력기기전선`을 `전력기기·전선`으로 통합
  - HD현대일렉트릭·SK이터닉스는 `에너지`, 리노공업·한미반도체·이오테크닉스는 `반도체 장비`, 알테오젠은 `바이오`로 지정
  - KBI메탈·가온전선·대원전선·대한항공·매드업·삼성E&A·삼호개발의 `기타` 분류를 명확한 업종으로 지정
  - 기존 DB의 동일 종목 및 통합 대상 업종 기록도 함께 보정해 과거·최근 섹터 흐름에 즉시 반영
---

## [2026-07-02 22:15] 최근 섹터 순위 표 포함 종목 및 모바일 표시 개선
- **작업 목적:** 최근 섹터 거래대금 순위에서 각 섹터를 구성한 종목을 확인하고 모바일에서도 종목명이 가려지지 않도록 개선했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 날짜·섹터별 TOP60 포함 종목명을 중복 없이 집계
  - 최근 섹터 순위 하단 표에 `포함 종목` 열 추가
  - 하단 표를 화면 폭에 맞춰 셀 내용이 자동 줄바꿈되는 반응형 HTML 표로 변경
  - 그래프 구성과 툴팁은 변경하지 않음
---

## [2026-07-02 22:05] 최근 섹터 흐름 거래대금 순위 그래프로 변경
- **작업 목적:** 외인·기관 합산 순매수 금액 대신 최근 며칠간 각 섹터의 거래대금 순위 변화를 직관적으로 확인할 수 있도록 개선했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 정규장 TOP60 데이터를 기준으로 날짜별 전체 섹터 합산 거래대금 순위 계산
  - 사용자가 선택한 이번 주 누적 거래대금 상위 N개 섹터의 일별 순위를 선 그래프로 표시하고 1위가 위쪽에 오도록 축 반전
  - 그래프 툴팁에 날짜·섹터·거래대금 순위·거래대금·종목 수 표시
  - 하단 최신 현황 표를 외인·기관 순매수 대신 거래대금 순위와 거래대금 중심으로 변경
---

## [2026-07-02 19:25] 수동 전체 분석 실제 실행시각 세션 분리
- **작업 목적:** 15:30~18:00 사이 웹 수동 분석이 실제 실행시각과 무관하게 `정규장(16:00)`으로 저장되는 문제를 해결했습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `ANALYSIS_RUN_MODE=full`인 웹 수동 분석을 `수동(HH:MM)` 세션으로 저장
  - Oracle 자동 09:30 실행은 `장중(09:30)`, 자동 16:00 실행은 `정규장(16:00)`으로 기존 이름 유지
  - 장 운영시간 밖의 수동 실행은 기존처럼 새 주가 세션을 만들지 않고 최신 정규장 데이터로 후속 분석
---

## [2026-07-02 19:15] GitHub Actions 중복 예약 실행 제거
- **작업 목적:** Oracle 타이머 도입 후에도 남아 있던 GitHub Actions 예약이 수시간 지연 실행되며 KIS 토큰을 추가 발급하고 시장강도 결과를 덮어쓰는 문제를 차단했습니다.
- **영향을 받은 파일:** `.github/workflows/main.yml`, `ai_changelog.md`
- **주요 변경 사항:**
  - GitHub Actions의 09:30·09:50·16:00 예약 cron 제거
  - 시장강도 탭의 수동 실행에 필요한 `workflow_dispatch`는 유지
  - Oracle VM의 기존 자동 실행 시간표와 분석 로직은 변경하지 않음
---

## [2026-07-01 01:02] Oracle 웹 수동 실행 세션 분리
- **작업 목적:** 웹버튼으로 자정에 실행한 분석이 자동 예약 실행으로 오인되어 `장중(09:30)` 세션에 저장된 문제를 해결했습니다.
- **영향을 받은 파일:** `cloud_job.py`, `oracle_trigger_server.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - Oracle 타이머의 `full-analysis`와 웹버튼의 `manual-analysis` 작업을 명시적으로 분리
  - 웹 수동 실행은 예약 cron 값을 제거하고 `ANALYSIS_RUN_MODE=full`로 실행해 버튼 요청 시각 기준 분기를 사용
  - 기존 09:30·16:00 자동 실행의 세션 지정 방식과 시간표는 변경하지 않음
  - 장 운영시간 밖의 웹 수동 실행은 새 주가 세션을 저장하지 않고 최신 정규장 DB로 후속 분석만 실행
---

## [2026-07-01 00:49] KIS 거래대금 주식 TOP60 후보 수집 보강
- **작업 목적:** KIS 거래대금 순위 API가 첫 30건만 반환하고 ETF·ETN 제거 후 10종목만 남아 Oracle 수동 분석이 중단되는 문제를 해결했습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 거래대금 TOP60의 선정·정렬 기준과 60개 검증 안전장치는 그대로 유지
  - KIS 공식 10자리 대상 제외 조건으로 ETF·ETN·SPAC을 요청 단계에서 제외
  - 비중복 가격 구간별로 거래금액순 후보를 조회하고 숨겨진 후보가 현재 60위보다 클 수 있는 구간만 재분할해 호출량을 제한
  - 구간별 결과를 합친 뒤 기존처럼 전체 거래대금 내림차순으로 정확히 60종목 확정
---

## [2026-07-01 00:41] Oracle 수집·뉴스 모듈 Python 3.8 호환성 보완
- **작업 목적:** Oracle 수동 분석에서 `crawler.py` import 시 남아 있던 `zoneinfo` 오류로 실제 분석이 중단되는 문제를 해결했습니다.
- **영향을 받은 파일:** `crawler.py`, `news_collector.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 전체 루트 Python 파일을 재검사해 남아 있던 `crawler.py`와 `news_collector.py`의 `ZoneInfo` 사용 확인
  - 두 모듈의 KST 표현만 Python 3.8 표준 라이브러리에서 지원하는 UTC+9 고정 시간대로 교체
  - 주가 수집과 뉴스 분석 로직은 변경하지 않음
---

## [2026-07-01 00:24] Oracle Python 3.8 시간대 호환성 수정
- **작업 목적:** Ubuntu 20.04의 Python 3.8에서 `zoneinfo` 모듈을 찾지 못해 수동 실행 API와 예약 분석이 시작되지 않는 문제를 해결했습니다.
- **영향을 받은 파일:** `oracle_trigger_server.py`, `cloud_job.py`, `program_ws_collector.py`, `market_strength.py`, `main.py`, `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - Python 3.9부터 제공되는 `zoneinfo.ZoneInfo`를 Python 3.8 표준 라이브러리의 UTC+9 고정 시간대로 교체
  - 수동 API뿐 아니라 Oracle VM에서 실행되는 전체 분석·시장강도·WebSocket 예약 작업의 동일 오류를 함께 방지
  - 데이터 수집, 분석, 점수 계산 로직은 변경하지 않음
---

## [2026-07-01 00:15] 웹 버튼 Oracle 즉시 분석 연결
- **작업 목적:** 웹페이지의 `지금 분석 실행` 버튼이 GitHub Actions 대기열을 거치지 않고 Oracle VM에서 버튼 요청 시각 기준 전체 분석을 즉시 시작하도록 연결했습니다.
- **영향을 받은 파일:** `app.py`, `oracle_trigger_server.py`, `.gitignore`, `deploy/oracle-cloud/stock-trigger.service`, `deploy/oracle-cloud/setup.sh`, `deploy/oracle-cloud/README.md`, `ai_changelog.md`
- **주요 변경 사항:**
  - Oracle VM에 수동 분석 요청과 실행 상태 조회를 처리하는 상시 HTTP 서비스 추가
  - HMAC 서명, 5분 시각 제한, 일회용 nonce로 요청을 검증해 비밀 토큰을 직접 전송하지 않도록 구성
  - 동시에 여러 분석이 실행되지 않도록 차단하고 실행 중·완료·실패 상태 및 진행률을 웹 사이드바에 표시
  - 웹의 전체 분석 버튼을 Oracle 직접 실행으로 교체하고 기존 GitHub Actions 시장강도 전용 실행은 유지
  - Oracle 설치 스크립트에서 수동 실행 서비스를 자동 설치·활성화하도록 구성
---

## [2026-06-29 23:47] 무료 클라우드 WebSocket 장중 수집기 추가
- **작업 목적:** GitHub Actions 예약 지연 없이 프로그램·차익·비차익 순매수를 목표 시각에 저장하고, 개인 PC가 꺼져 있어도 시장강도와 전체 분석을 자동 실행할 수 있는 무료 VM 구성을 추가했습니다.
- **영향을 받은 파일:** `program_ws_collector.py`, `cloud_job.py`, `market_strength.py`, `requirements.txt`, `deploy/oracle-cloud/*`, `ai_changelog.md`
- **주요 변경 사항:**
  - KIS 국내지수 실시간 프로그램매매 WebSocket(`H0UPPGM0`)을 연결해 오전 09:15/09:30/09:45 및 종가 14:30/15:00/15:20/15:30 스냅샷 저장
  - WebSocket 원시값을 VM 전용 `program_snapshots.db`에 분리하고 시장강도 계산 시 완전한 스냅샷을 REST 값보다 우선 사용
  - Oracle Cloud Always Free VM에서 `systemd timer`로 09:30/16:00 전체 분석, 오전/종가 수집 및 시장강도 계산을 직접 실행하는 작업기와 설치 스크립트 추가
  - 각 클라우드 작업의 DB 변경을 직렬화하고 완료 후 GitHub 저장소에 자동 커밋·푸시하도록 구성
  - Google Cloud 무료 VM은 외부 IPv4가 별도 과금되어 제외하고, Oracle 무료 인스턴스의 유휴 자원 회수 가능성을 설치 문서에 명시
---

## [2026-06-29 23:05] 수동 분석 장외 실행 및 상태 표시 수정
- **작업 목적:** 장 종료 후 `지금 분석 실행` 요청이 시간외 제외 규칙으로 즉시 끝나 웹에 아무 변화가 없던 문제와 변경 없는 DB 커밋 단계의 실패를 해결했습니다.
- **영향을 받은 파일:** `main.py`, `app.py`, `.github/workflows/main.yml`, `ai_changelog.md`
- **주요 변경 사항:**
  - 장중 수동 실행은 Actions 시작시각의 실시간 데이터를 수집하고, 장 운영시간 외 수동 실행은 최신 정규장 DB로 뉴스·종가 시장강도·종합분석·엑셀·텔레그램을 계속 실행하도록 분기
  - 웹 버튼 요청 성공/실패 메시지를 세션 상태에 유지하고 `실행 상태 새로고침` 버튼 추가
  - Actions 커밋 단계에서 실제 스테이징 변경만 검사하고 `reports` 폴더 존재 여부를 확인해 변경사항이 없을 때 정상 종료
---

## [2026-06-29 22:46] 지금 분석 실행 기준시각 통일
- **작업 목적:** 웹의 `지금 분석 실행` 버튼으로 실행되는 주가·수급·뉴스·시장강도·리포트가 GitHub Actions 실제 시작시각을 공통 기준으로 사용하도록 정리했습니다.
- **영향을 받은 파일:** `app.py`, `main.py`, `.github/workflows/main.yml`, `ai_changelog.md`
- **주요 변경 사항:**
  - GitHub Actions 수동 실행 입력에 `full`과 `market_strength_only` 실행 모드 추가
  - `full` 실행 시 Actions 시작 KST 시각을 시장강도 수동 분석 기준으로 적용해 현재·15분 전·30분 전 흐름 분석
  - 사이드바 `지금 분석 실행`은 전체 자동화, 시장강도 탭 버튼은 시장강도 전용 자동화로 분리
  - 웹 실행 안내를 버튼 클릭시각이 아닌 GitHub Actions 실제 시작시각 기준으로 명확화
---

## [2026-06-29 22:35] 시장강도 KIS 토큰 재사용 및 오류 표시 개선
- **작업 목적:** 종목 수집 직후 시장강도 분석이 KIS 토큰을 중복 발급해 1분당 발급 제한에 걸리고 DB가 비어 있던 문제를 해결했습니다.
- **영향을 받은 파일:** `main.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `run_market_strength`: 종목 수집에 사용한 KIS 접근토큰을 시장강도 분석기에 전달해 중복 토큰 발급 방지
  - `report_market_strength_error`: 시장강도 분석 실패 시 GitHub Actions Annotations에 오류 원인이 표시되도록 추가
  - KST 09:50 시장강도 전용 실행은 독립 토큰을 한 번만 발급하는 기존 흐름 유지
---

## [2026-06-29 22:23] 거래대금 TOP60 수집 및 자동 실행 시간 수정
- **작업 목적:** KIS 거래대금 순위가 일부 페이지만 수집되어 TOP60이 누락되는 문제를 해결하고, 일반 분석 예약 시간을 09:30/16:00으로 정리했습니다.
- **영향을 받은 파일:** `crawler.py`, `main.py`, `.github/workflows/main.yml`, `ai_changelog.md`
- **주요 변경 사항:**
  - `StockCrawler.get_market_data`: KIS `tr_cont` 연속 조회를 적용하고 전체 주식·우선주에서 ETF/ETN 등을 제외한 뒤 거래대금 내림차순 상위 60종목만 확정
  - 거래대금 상위 주식이 정확히 60종목 수집되지 않으면 불완전한 데이터를 DB에 저장하지 않고 실행을 중단하도록 검증 추가
  - 일반 종목 분석 예약을 KST 09:30/16:00으로 변경하고, KST 09:50 실행은 오전 시장강도 분석만 수행하도록 분리
---

## [2026-06-27 02:06] 업종 통합 및 업종 묶음 표 줄바꿈 개선
- **작업 목적:** 사용자가 지정한 업종 분류를 통합하고, 업종별 종목 묶음 표에서 긴 종목 목록이 잘려 보이는 문제를 개선했습니다.
- **영향을 받은 파일:** `crawler.py`, `app.py`, `stock_data.db`, `ai_changelog.md`
- **주요 변경 사항:**
  - `StockCrawler._normalize_sector`: 브이엠/티에스이/피에스케이홀딩스를 `반도체 장비`, LG를 `밸류업금융`으로 지정하고 전력/바이오/기판/화장품/에너지 계열 업종명을 표준 업종으로 통합
  - `stock_data.db`: 기존 누적 데이터 1,626건의 업종을 요청 기준으로 병합하고 지정 종목 업종값을 갱신
  - `display_wrapped_table`: 업종별 종목 묶음 표를 줄바꿈 가능한 HTML 표로 표시해 긴 종목 목록이 화면 밖으로 잘리는 문제 완화
---

## [2026-06-27 01:52] 자동 실행 스케줄 12시/16시로 단순화
- **작업 목적:** GitHub Actions 예약 실행 지연으로 장중 촘촘한 자동 실행이 실제 의도한 시간에 돌지 않는 문제를 줄이기 위해 자동 실행 시간을 단순화했습니다.
- **영향을 받은 파일:** `.github/workflows/main.yml`, `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - GitHub Actions 예약 실행을 KST 12:00, KST 16:00 두 번만 남기고 기존 09:05/09:20/10:00/14:00/14:30/15:40/16:10 스케줄 제거
  - `StockCrawler._get_session_name`: `0 3 * * 1-5`는 `장중(12:00)`, `0 7 * * 1-5`는 `정규장(16:00)`으로 저장되도록 예약 라벨 정리
  - 16:00 실행은 기존 정규장 마감 요약과 시장강도 분석을 함께 수행하고, 12:00 실행에서는 시장강도 분석이 15:35 이전 조건으로 자동 skip되도록 유지
---

## [2026-06-27 01:46] 시장 강도 분석 탭 추가
- **작업 목적:** 종가베팅 전 한국 시장의 장 막판 수급 환경을 판단할 수 있도록 시장강도 분석 수집/점수화/웹 표시 기능을 추가했습니다.
- **영향을 받은 파일:** `market_strength.py`, `main.py`, `app.py`, `.github/workflows/main.yml`, `ai_changelog.md`
- **주요 변경 사항:**
  - `MarketStrengthAnalyzer`: KIS API 기반으로 14:30, 15:00, 15:20, 15:30의 베이시스/프로그램매매/코스피200 선물 흐름을 수집하고 100점 만점 시장강도 점수를 계산하는 모듈 추가
  - 시장강도 배점을 `베이시스 35점`, `프로그램매매 35점`, `코스피200 선물 추세 30점`으로 적용하고, 15:35 이전 실행은 자동으로 건너뛰도록 처리
  - `main.py` 자동화 흐름에 시장강도 분석 단계를 추가하고, GitHub Actions에 KST 15:40 예약 실행을 추가
  - 웹 대시보드에 `시장 강도 분석` 탭을 추가해 상단 점수/상태, AI 해석 문구, 항목별 점수 카드, 시간별 흐름 테이블을 표시
---

## [2026-06-10 01:08] 로봇/건설 업종명 통일
- **작업 목적:** 업종별 묶음에서 `로봇`/`로봇주`, `건설`/`건설주`가 따로 나뉘어 보이지 않도록 업종명을 통일했습니다.
- **영향을 받은 파일:** `crawler.py`, `stock_data.db`, `ai_changelog.md`
- **주요 변경 사항:**
  - `StockCrawler._normalize_sector`: 휴림로봇 및 로봇 포함 종목은 `로봇`, 건설 포함 종목은 `건설`로 저장되도록 매핑 수정
  - `stock_data.db`: 기존 `로봇주` 14건을 `로봇`으로, `건설주` 142건을 `건설`로 병합
  - DB 확인 결과 `건설` 164건, `로봇` 594건으로 통일되고 `건설주`/`로봇주` 그룹은 제거됨
---

## [2026-06-10 01:04] 웹 대시보드 최신 원격 DB 우선 로딩
- **작업 목적:** Streamlit 배포본이 오래된 로컬 `stock_data.db` 또는 캐시를 읽어 최신 날짜가 웹페이지에 보이지 않는 문제를 줄이기 위해 최신 GitHub DB를 우선 읽도록 개선했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `get_database_path`: GitHub raw `stock_data.db`를 임시 파일로 내려받아 SQLite DB로 사용하는 공통 로딩 함수 추가
  - `get_analyzed_data`, `get_raw_data`, `get_news_data`: 모두 같은 최신 DB 경로를 사용하도록 변경
  - 데이터 캐시 TTL을 10분에서 1분으로 줄여 수동 실행 후 최신 데이터 반영 지연을 완화
---

## [2026-06-10 00:58] 수동 분석 실행 상태 표시 추가
- **작업 목적:** 웹페이지에서 `지금 분석 실행` 버튼을 누른 뒤 GitHub Actions 실행이 대기중/실행중/완료인지 확인할 수 있도록 개선했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `get_latest_workflow_run`: GitHub Actions API에서 최신 workflow run 상태를 조회하는 함수 추가
  - `display_workflow_run_status`: 최신 실행의 상태를 진행 바로 표시하고 실행 번호, 마지막 갱신 시각, GitHub Actions 상세 링크를 함께 보여주도록 추가
  - 수동 분석 실행 버튼 아래에 최근 실행 상태 영역을 배치해 완료 여부를 바로 확인할 수 있도록 구성
---

## [2026-06-10 00:50] 최신 DB 복구 및 업종 보정 재적용
- **작업 목적:** 이전 DB 충돌 처리 과정에서 최신 누적 데이터가 오래된 DB로 되돌아간 문제를 복구했습니다.
- **영향을 받은 파일:** `stock_data.db`, `ai_changelog.md`
- **주요 변경 사항:**
  - 자동 업데이트 커밋 `8ce3118e`의 최신 `stock_data.db`를 복원해 `20260609` 데이터가 다시 포함되도록 조치했습니다.
  - 복원된 최신 DB 위에 디앤디파마텍/건설주/화장품주/로봇주 업종 보정을 다시 적용했습니다.
---

## [2026-06-10 00:04] 웹페이지 수동 분석 실행 버튼 추가
- **작업 목적:** GitHub Actions 예약 실행이 지연될 때 사용자가 웹페이지에서 직접 현재 시각 기준 분석 실행을 요청할 수 있도록 버튼을 추가했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `trigger_github_workflow`: GitHub REST API의 workflow dispatch 엔드포인트를 호출해 `main.yml` 자동화를 수동 실행하는 함수 추가
  - 사이드바에 `지금 분석 실행` 버튼을 추가하고, 실행 요청 성공/실패 메시지를 표시하도록 구성
  - 웹 배포 환경의 `GITHUB_ACTIONS_TOKEN`, `GITHUB_REPOSITORY`, `GITHUB_WORKFLOW_FILE`, `GITHUB_BRANCH` 설정을 읽어 실행 대상 저장소/워크플로를 지정할 수 있도록 처리
---

## [2026-06-09 23:51] 뉴스 이슈 탭 제목 표시 개선
- **작업 목적:** 뉴스 이슈 종목 탭에서 긴 뉴스 제목이 표 안에서 한 줄로 붙어 보여 가독성이 떨어지는 문제를 개선했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 뉴스 요약 표에서 긴 `최신 뉴스` 제목 컬럼을 제거하고 `뉴스 수` 컬럼을 추가했습니다.
  - 종목별 뉴스 펼쳐보기에서 각 뉴스 제목을 번호가 붙은 별도 줄로 표시하고, 제목 클릭 시 원문 링크로 이동하도록 변경했습니다.
  - 뉴스별 감정, 출처, 발행시각은 제목 아래 보조 정보로 분리해 표시하도록 정리했습니다.
---

## [2026-06-09 23:39] 업종 보정 및 상승 종목 상승률 표시
- **작업 목적:** 사용자가 지정한 업종 분류 오류를 보정하고, 상승 종목 업종 묶음 표에서 종목별 상승률을 함께 확인할 수 있도록 개선했습니다.
- **영향을 받은 파일:** `crawler.py`, `app.py`, `stock_data.db`, `ai_changelog.md`
- **주요 변경 사항:**
  - `StockCrawler._normalize_sector`: 디앤디파마텍/생물공학 업종을 바이오로, 건설 포함 종목을 건설주로, 에이피알 및 주요 화장품 종목을 화장품주로, 휴림로봇 및 로봇 포함 종목을 로봇주로 보정했습니다.
  - `display_sector_summary`: 상승 종목 묶음 표에서만 종목명 옆에 등락률을 `(+0.00%)` 형식으로 표시하도록 옵션을 추가했습니다.
  - `stock_data.db`: 기존 누적 데이터 중 디앤디파마텍, 현대건설, 대우건설, 에이피알 등 이번 보정 대상 종목의 업종값을 갱신했습니다.
---

## [2026-06-07 23:31] 뉴스 이슈 종목 탭 추가
- **작업 목적:** 거래대금 상위 종목 중 뉴스 이슈가 붙은 종목을 종목별로 압축해서 볼 수 있도록 뉴스 수집과 대시보드 탭을 추가했습니다.
- **영향을 받은 파일:** `news_collector.py`, `main.py`, `app.py`, `stock_data.db`, `ai_changelog.md`
- **주요 변경 사항:**
  - Google News RSS 기반으로 거래대금 Top 60 종목의 뉴스 제목, 링크, 언론사, 발행시각을 수집해 `stock_news` 테이블에 저장하는 `NewsCollector`를 추가했습니다.
  - 기존 DB에 `stock_news` 테이블을 추가했습니다.
  - 뉴스 제목을 긍정/부정/중립 키워드 룰로 분류하고, 종목별 뉴스 점수와 주요 키워드를 계산할 수 있도록 저장했습니다.
  - 자동 실행 흐름에 뉴스 수집 단계를 추가하되, 뉴스 수집 실패가 주가 분석/텔레그램 전송을 막지 않도록 예외 처리했습니다.
  - 웹 대시보드에 `뉴스 이슈 종목` 탭을 추가해 종목별 요약표와 종목별 뉴스 펼쳐보기를 제공하도록 했습니다.
---

## [2026-06-07 01:57] 추가 지정 종목 섹터 보정
- **작업 목적:** 사용자가 추가 지정한 서진시스템, 삼성전자우, 현대차 우선주 계열이 원하는 섹터로 묶이도록 조정했습니다.
- **영향을 받은 파일:** `crawler.py`, `stock_data.db`, `ai_changelog.md`
- **주요 변경 사항:**
  - `서진시스템` 섹터 매핑을 `2차전지`로 변경했습니다.
  - `삼성전자우` 섹터 매핑을 `반도체 메모리`로 추가했습니다.
  - `현대차우`, `현대차2우B`, `현대차3우B` 섹터 매핑을 `로봇`으로 변경/추가하고, 기존 DB에 있는 행을 업데이트했습니다.
---

## [2026-06-07 01:52] 웹 표 메타데이터 컬럼 숨김
- **작업 목적:** 실제 수집시각, 데이터 출처, 예약 실행값 같은 검증용 메타데이터가 웹 표를 복잡하게 만들지 않도록 화면 표시에서 숨겼습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 공통 표 표시 함수에서 `collected_at_kst`, `data_source`, `scheduled_cron` 컬럼을 기본 숨김 처리했습니다.
  - CSV 다운로드에는 메타데이터가 계속 포함되도록 데이터 원본과 다운로드 로직은 변경하지 않았습니다.
---

## [2026-06-07 01:41] 로봇 섹터 지정 종목 추가 보정
- **작업 목적:** 사용자가 추가 지정한 현대차·기아·현대오토에버·현대모비스·LG전자·LG씨엔에스가 업종별 묶음 보기에서 로봇 섹터로 보이도록 조정했습니다.
- **영향을 받은 파일:** `crawler.py`, `stock_data.db`, `ai_changelog.md`
- **주요 변경 사항:**
  - `현대차`, `기아`, `현대모비스`, `현대오토에버`, `LG전자`, `LG씨엔에스`의 섹터 매핑을 `로봇`으로 변경했습니다.
  - 기존 DB에 저장되어 있던 해당 종목 행 중 존재하는 항목만 `로봇` 섹터로 업데이트했습니다.
  - 현재 DB에 없는 `현대오토에버`는 앞으로 수집될 때 `로봇`으로 분류되도록 매핑에 추가했습니다.
---

## [2026-06-07 01:35] 지정 종목 섹터 매핑 보정
- **작업 목적:** 업종별 종목 묶음 보기에서 사용자가 지정한 종목들이 시장에서 보는 대표 섹터로 묶이도록 섹터 보정 매핑을 추가했습니다.
- **영향을 받은 파일:** `crawler.py`, `stock_data.db`, `ai_changelog.md`
- **주요 변경 사항:**
  - 로봇, 2차전지, 태양광·수소, 반도체, 기판, 광통신, 방산·우주항공, 전력기기·전선, 모빌리티, 바이오·헬스, 원전, 조선, 밸류업금융, 플랫폼·IT, IT부품·산업재 지정 종목의 exact-name 섹터 매핑을 추가했습니다.
  - 지정 목록에 포함된 종목명만 우선 보정하고, 그 외 종목은 기존 티커/키워드/네이버 업종 기반 분류를 그대로 유지하도록 했습니다.
  - 기존 DB에 저장되어 있던 지정 종목 행도 같은 섹터 매핑으로 업데이트했습니다.
  - 한 종목당 `sector`가 하나만 저장되는 현재 구조에 맞춰 중복 지정 종목은 대표 섹터 하나로 저장되도록 했습니다.
---

## [2026-06-07 00:29] 거래대금 Top 탭 보조 분석 표 추가
- **작업 목적:** 거래대금 상위 종목 중 어떤 업종이 상승했는지와 외인·기관이 함께 매수한 종목을 빠르게 확인할 수 있도록 보조 표를 추가했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 거래대금 Top 탭에 등락률이 양수인 종목만 모아 업종별로 묶어 보여주는 `상승 종목 업종별 묶음 보기`를 추가했습니다.
  - 거래대금 Top 60 안에서 외인 순매수와 기관 순매수가 모두 양수인 종목을 보여주는 `외인·기관 동시 순매수 종목` 표를 추가했습니다.
  - 기존 업종 묶음 함수가 다른 제목을 받을 수 있도록 선택 인자를 추가했습니다.
---

## [2026-06-06 23:24] 주요 탭 표 컬럼 및 기본 정렬 정리
- **작업 목적:** 거래대금 Top, 외인 순매수, 기관 순매수 탭에서 필요한 정보만 빠르게 볼 수 있도록 표 표시와 기본 정렬을 정리했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `종가` 컬럼 표시명을 `현재가`로 변경했습니다.
  - 거래대금/외인/기관 탭 표에서 `날짜`, `시간`, `종목코드`, `거래량`, `테마` 열을 숨기도록 했습니다.
  - 거래대금 Top 탭은 `거래대금` 내림차순, 외인/기관 탭은 각각 `외인 순매수`, `기관 순매수` 내림차순으로 기본 표시되도록 했습니다.
---

## [2026-06-06 23:04] 시간외 데이터 수집 및 표시 제외
- **작업 목적:** NXT 공개 페이지 기반 시간외 데이터의 범위와 거래대금 의미가 불명확해 분석 결과를 흐릴 수 있어 시간외 데이터를 자동 분석 대상에서 제외했습니다.
- **영향을 받은 파일:** `.github/workflows/main.yml`, `crawler.py`, `main.py`, `analyzer.py`, `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - GitHub Actions의 KST `20:30` 시간외 자동 실행 스케줄을 제거했습니다.
  - 수동 실행이나 지연 실행으로 시간외 세션이 잡혀도 크롤러가 저장하지 않고 종료하도록 방어했습니다.
  - 시간외 세션이 제외될 때 분석, 엑셀 생성, 텔레그램 전송까지 이어지지 않도록 메인 실행 흐름을 중단했습니다.
  - 분석기와 웹 대시보드가 기존 DB에 남아 있는 시간외 행을 읽지 않도록 필터링했습니다.
  - 웹 대시보드의 `정규장 vs 시간외` 비교 탭을 제거했습니다.
---

## [2026-06-06 22:50] 데이터 수집 시각 추적 및 실행 안정화
- **작업 목적:** 오전/장후 데이터가 실제 언제 수집된 값인지 검증할 수 있도록 저장 메타데이터를 추가하고, 정규장 마감 실행 누락 가능성을 줄였습니다.
- **영향을 받은 파일:** `crawler.py`, `.github/workflows/main.yml`, `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `daily_stocks`에 `collected_at_kst`, `data_source`, `scheduled_cron` 컬럼을 추가해 실제 수집시각, 데이터 출처, 예약 실행값을 저장하도록 했습니다.
  - `20:30` 예약 실행은 실제 실행 시각과 무관하게 `시간외(20:30)` 및 `NXT` 출처로 저장되도록 고정했습니다.
  - 정규장 마감 예약 실행을 KST `16:10`으로 늦추되 DB에는 `정규장(16:00)`으로 저장하고, 자동 실행 스크립트에 3회 재시도를 추가했습니다.
  - 웹 대시보드 컬럼 매핑에 수집 메타데이터 표시명을 추가했습니다.
---

## [2026-06-03 01:59] 대시보드 세션 문구 시간으로 변경
- **작업 목적:** 사용자가 날짜와 실행 시간을 선택해 데이터를 내려받는 구조를 더 직관적으로 이해할 수 있도록 웹 대시보드의 표시 문구를 정리했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 사이드바 선택 라벨과 다운로드 버튼명을 `세션` 대신 `시간` 기준 문구로 변경했습니다.
  - KPI와 직전 비교 탭의 화면 문구도 `선택된 시간`, `직전 시간 대비`로 맞췄습니다.
  - 내부 데이터 컬럼과 로직은 그대로 유지하고 사용자에게 보이는 텍스트만 수정했습니다.
---

## [2026-06-03 01:51] 웹 대시보드 CSV 다운로드 추가
- **작업 목적:** 모바일 웹페이지에서 주가 분석 데이터를 CSV로 내려받아 ChatGPT 등에 업로드해 추가 분석할 수 있도록 했습니다.
- **영향을 받은 파일:** `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 사이드바에 `선택 세션 CSV`, `선택 날짜 CSV`, `전체 데이터 CSV` 다운로드 버튼을 추가했습니다.
  - CSV는 원본 숫자 데이터를 유지하고, 한글 호환성을 위해 `utf-8-sig` 인코딩으로 생성하도록 했습니다.
  - 세션명이 포함된 파일명을 모바일에서도 안전하게 저장할 수 있도록 파일명 정리 함수를 추가했습니다.
---

## [2026-06-03 01:34] 시장 섹터 보정 확대 및 섹터별 자금 중복 제거
- **작업 목적:** 회사 신고 업종 기준으로 섞이던 종목들을 시장에서 보는 섹터/테마 기준으로 더 잘 묶고, 섹터별 자금 탭에서 종목 중복 집계를 제거했습니다.
- **영향을 받은 파일:** `crawler.py`, `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 2차전지/배터리, 전자부품/기판, 조선, 금융, 보험, 수소/신재생, 에너지/정유, 바이오/제약 등 주요 시장 섹터 매핑을 보강했습니다.
  - 업종별 종목 묶음 보기에서 같은 ticker가 중복 집계되지 않도록 `ticker` 기준 중복 제거 후 요약하도록 변경했습니다.
  - 섹터별 자금 탭은 전체 카테고리 합산이 아니라 `VOLUME_TOP_60` 종목만 기준으로 섹터별 거래대금/수급을 집계하도록 변경했습니다.
---

## [2026-06-03 00:59] NXT 시간외 거래대금 수집 1차 구현
- **작업 목적:** 20:30 시간외 세션에서 KIS 정규장 데이터를 재저장하지 않고, 넥스트레이드 공개 페이지의 NXT 거래대금 상위 데이터를 우선 저장하도록 변경했습니다.
- **영향을 받은 파일:** `crawler.py`, `analyzer.py`, `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `StockCrawler.get_nxt_aftermarket_data()`를 추가해 넥스트레이드 메인 페이지의 거래대금 상위 표를 파싱하도록 했습니다.
  - `시간외(20:30)` 세션에서는 NXT 거래대금 데이터만 `VOLUME_TOP_60`에 저장하고, NXT에서 제공되지 않는 외인/기관 Top 카테고리는 빈 데이터로 정리하도록 했습니다.
  - DB 저장 전에 같은 날짜/세션/카테고리의 기존 행을 삭제해 이전 실행의 잔여 종목이 섞이지 않도록 했습니다.
  - 웹/텔레그램 표시에서 `시간외(20:30)`을 `NXT 시간외(20:30)`으로 구분해 보이도록 했습니다.
---

## [2026-06-03 00:39] 시간외 자정 지연 날짜 보정
- **작업 목적:** 20:30 시간외 자동 실행이 GitHub Actions 지연으로 자정을 넘겨 완료될 때 다음 날짜로 저장되는 문제를 수정했습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `StockCrawler._resolve_target_date()`를 추가해 날짜 계산을 별도 함수로 분리했습니다.
  - `20:30` 예약 실행이 KST 자정 이후 새벽 시간대에 실행되면 직전 영업일을 `target_date`로 사용하도록 했습니다.
  - 일반 장중/정규장 실행은 기존처럼 현재 KST 기준 최근 영업일을 사용하도록 유지했습니다.
---

## [2026-06-03 00:28] 거래대금 순위 기준 및 시간외 세션 보정
- **작업 목적:** 거래대금 Top 60 후보가 실제 거래대금순이 아닌 기준으로 수집될 수 있는 문제와, 20:30 이전 실행이 시간외 확정 세션으로 저장될 수 있는 문제를 수정했습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - KIS 거래대금 순위 API의 `FID_BLNG_CLS_CODE`를 거래금액순 기준인 `3`으로 변경했습니다.
  - 거래대금 후보에서 ETF/ETN 섞임을 줄이기 위해 `FID_DIV_CLS_CODE`를 보통주 기준인 `1`로 변경했습니다.
  - 20:30 예약 세션이라도 실제 KST 시간이 20:30 이전이면 `시간외진행(HH:MM)`으로 저장하고, 20:30 이후에만 `시간외(20:30)`으로 저장하도록 했습니다.
---

## [2026-06-02 00:24] 주요 종목 섹터 보정 추가
- **작업 목적:** 네이버 업종 크롤링 결과가 너무 넓거나 부정확해 업종별 종목 묶음이 어색하게 보이는 문제를 줄였습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `StockCrawler._normalize_sector()`를 추가해 주요 종목을 반도체, 반도체 장비, 로봇, 전력/전기장비, 방산, 자동차, 인터넷/플랫폼 등 주도 테마 기준 섹터로 보정했습니다.
  - 네이버에서 가져온 업종은 기본값으로 유지하되, 티커 기준 매핑과 종목명 키워드 매핑에 해당하는 경우 보정 섹터를 우선 적용하도록 했습니다.
  - 카테고리별 저장 전에 섹터 보정을 적용해 웹페이지의 업종별 종목 묶음 보기와 섹터별 자금 요약이 더 일관되게 보이도록 했습니다.
---

## [2026-06-02 00:11] 금액 표시 정렬 및 수급 단위 보정
- **작업 목적:** 웹 표 정렬이 숫자 기준으로 동작하도록 표 안의 `억` 문자열을 제거하고, 외인/기관 순매수 금액이 0처럼 보이던 단위 변환 문제를 수정했습니다.
- **영향을 받은 파일:** `app.py`, `analyzer.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 웹 대시보드 표의 금액 컬럼은 숫자만 표시하고, 컬럼명에 `(억)` 단위를 붙이도록 변경했습니다.
  - 시가총액/거래대금은 원 단위에서 억 단위 정수로, 외인/기관 순매수는 KIS 수급 금액 단위에 맞춰 억 단위 정수로 변환하도록 분리했습니다.
  - 텔레그램 브리핑도 소수점 없이 억 단위로 표시하고, 외인/기관 순매수 금액은 보정된 단위로 표시하도록 수정했습니다.
---

## [2026-06-01 23:58] 웹/텔레그램 금액 억 단위 표시
- **작업 목적:** 웹페이지와 텔레그램 메시지에서 거래대금, 시가총액, 외인/기관 순매수 금액을 읽기 쉬운 억 단위로 통일했습니다.
- **영향을 받은 파일:** `app.py`, `analyzer.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 웹 대시보드 표 표시 전에 `market_cap`, `trading_value`, `foreign_net`, `inst_net` 값을 억 단위 문자열로 변환하도록 추가했습니다.
  - 섹터 요약과 직전 세션 대비 거래대금 비교 표도 억 단위로 표시되도록 정리했습니다.
  - 텔레그램 브리핑에 섹터 거래대금, 신규 진입 종목의 거래대금/외인/기관 순매수, 거래급증 종목의 거래대금을 억 단위로 표시하도록 추가했습니다.
---

## [2026-06-01 23:40] 상품형 종목 분석 대상 제외
- **작업 목적:** 거래대금/수급 상위 목록에 ETF, ETN, 레버리지, 인버스 등 상품형 종목이 섞여 추천 정확도가 낮아지는 문제를 줄였습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `StockCrawler._exclude_exchange_traded_products()`를 추가해 종목명 기준으로 상품형 종목을 제외하도록 했습니다.
  - 시장 데이터와 수급 데이터를 병합하고 결측값을 정리한 뒤, 카테고리별 Top 목록을 만들기 전에 상품형 종목 필터를 적용했습니다.
  - 제외된 상품형 종목 수를 실행 로그에 출력하도록 했습니다.
---

## [2026-06-01 23:16] 자동 분석 세션 시간 고정
- **작업 목적:** GitHub Actions 실행 지연으로 DB/웹페이지/텔레그램에 실제 실행 시간이 세션명으로 저장되는 문제를 수정했습니다.
- **영향을 받은 파일:** `crawler.py`, `.github/workflows/main.yml`, `ai_changelog.md`
- **주요 변경 사항:**
  - GitHub Actions의 예약 cron 값을 `GITHUB_EVENT_SCHEDULE` 환경변수로 Python 실행 단계에 전달하도록 추가했습니다.
  - `StockCrawler._get_session_name()`을 추가해 예약 cron 값을 `장중(09:05)`, `정규장(16:00)`, `시간외(20:30)` 같은 고정 세션명으로 매핑하도록 했습니다.
  - 수동 실행처럼 예약 cron 값이 없는 경우에는 기존 방식대로 현재 KST 시각 기준 세션명을 사용하도록 유지했습니다.
---

## [2026-05-29 00:57] KIS 상장주식수 기반 시가총액 계산 추가
- **작업 목적:** 시장 데이터 저장 시 `market_cap`이 항상 0으로 저장되던 문제를 해결했습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - KIS 거래량 순위 API 응답의 `lstn_stcn` 값을 `listed_shares`로 매핑했습니다.
  - `close * listed_shares` 방식으로 `market_cap`을 계산하도록 변경했습니다.
  - 수급 상위 종목의 개별 시세 보강 조회에서도 `lstn_stcn`이 응답될 경우 시가총액을 함께 채우도록 했습니다.
---

## [2026-05-29 00:53] 수급 병합 종목명 보존 및 결측 처리 개선
- **작업 목적:** 시장 데이터와 KIS 수급 데이터를 병합할 때 종목명이 `name_x/name_y`로 분리된 뒤 저장 시 빈 값으로 남는 문제를 해결했습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - 시장 데이터명과 수급 데이터명을 병합 직후 하나의 `name` 컬럼으로 통합하도록 추가했습니다.
  - 전체 DataFrame 결측 처리에서 문자 컬럼(`ticker`, `name`, `sector`, `theme`)과 숫자 컬럼을 분리해, 종목명이 0으로 덮이지 않도록 변경했습니다.
  - 수급 상위 종목처럼 시장 데이터에 없는 종목도 KIS 수급 API의 종목명을 유지하도록 했습니다.
---

## [2026-05-29 00:38] Actions rebase 자동 stash 적용
- **작업 목적:** 자동 커밋 후 남아 있는 unstaged 변경 때문에 `git pull --rebase`가 실패하던 문제를 해결했습니다.
- **영향을 받은 파일:** `.github/workflows/main.yml`, `ai_changelog.md`
- **주요 변경 사항:**
  - GitHub Actions의 DB/리포트 저장 step에서 `git pull --rebase origin main`을 `git pull --rebase --autostash origin main`으로 변경했습니다.
  - rebase 전 남아 있는 작업 트리 변경을 자동으로 임시 보관했다가 다시 적용할 수 있게 했습니다.
---

## [2026-05-29 00:21] GitHub Actions 자동 커밋 푸시 충돌 완화
- **작업 목적:** 자동 실행이 DB/리포트 커밋을 만든 뒤 원격 `main`에 새 커밋이 있어 `git push`가 거절되는 문제를 줄였습니다.
- **영향을 받은 파일:** `.github/workflows/main.yml`, `ai_changelog.md`
- **주요 변경 사항:**
  - GitHub Actions의 DB/리포트 저장 step을 `if` 블록으로 정리해 변경사항이 있을 때만 커밋/푸시하도록 명확히 했습니다.
  - 자동 커밋 생성 후 `git pull --rebase origin main`을 수행한 뒤 `git push`하도록 변경해 원격 브랜치 선행 커밋을 반영하게 했습니다.
---

## [2026-05-29 00:14] 수급 데이터 수집 KIS API 전환
- **작업 목적:** 네이버 금융 크롤링에 의존하던 외국인/기관 수급 데이터 수집을 검증된 KIS `foreign-institution-total` API 기반으로 전환했습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `get_investor_data()`가 네이버 HTML 크롤링 대신 KIS `foreign-institution-total` API를 호출하도록 변경했습니다.
  - 외국인 순매수 상위와 기관 순매수 상위를 각각 조회해 `foreign_net`, `inst_net`, `ticker`, `name`으로 병합하도록 매핑했습니다.
  - KIS 수급 조회 실패 시 기존처럼 경고를 남기고 빈 수급 DataFrame을 반환해 자동화가 중단되지 않도록 유지했습니다.
---

## [2026-05-29 00:07] KIS 수급 API 후보 탐색 스크립트 확장
- **작업 목적:** 네이버 크롤링을 대체할 KIS 수급 API 후보를 찾기 위해 검증 스크립트가 여러 endpoint/TR 조합을 중단 없이 테스트하도록 확장했습니다.
- **영향을 받은 파일:** `test_kis_investor_api.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `ApiProbe` 구조를 추가해 여러 KIS API 후보를 순차 테스트할 수 있게 했습니다.
  - `foreign-institution-total`, `inquire-investor`, `investor-trend-estimate` 후보를 추가하고 HTTP 오류가 나도 다음 후보를 계속 검사하도록 변경했습니다.
  - 로컬 검증 결과 `foreign-institution-total` API에서 외국인/기관 순매수 금액 필드(`frgn_ntby_tr_pbmn`, `orgn_ntby_tr_pbmn`)가 정상 응답하는 것을 확인했습니다.
---

## [2026-05-28 23:55] KIS 투자자 수급 API 검증 스크립트 추가
- **작업 목적:** 네이버 크롤링을 KIS API 기반 수급 데이터로 대체할 수 있는지 안전하게 확인하기 위한 독립 검증 스크립트를 추가했습니다.
- **영향을 받은 파일:** `test_kis_investor_api.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `test_kis_investor_api.py`를 추가해 KIS 토큰 발급과 `investor-trend` API 호출을 분리 검증할 수 있게 했습니다.
  - API 키, 시크릿, 토큰은 출력하지 않고 응답 상태, 메시지, output 필드 구조와 첫 행 샘플만 출력하도록 구성했습니다.
  - 기존 자동화 실행 로직은 변경하지 않았습니다.
---

## [2026-05-28 23:51] KIS 거래량 순위 마켓 코드 정리
- **작업 목적:** GitHub Actions 실행 중 KIS 거래량 순위 API에서 유효하지 않은 `V` 시장 코드 호출로 경고가 발생하던 문제를 정리했습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `get_market_data()`의 거래량 순위 조회 시장 코드를 `['J', 'V']`에서 `['J']`로 변경했습니다.
  - `V` 코드는 해당 KIS 거래량 순위 API에서 유효하지 않으므로 호출하지 않도록 했습니다.
---

## [2026-05-28 23:44] 수급 크롤링 실패 시 자동화 중단 방지
- **작업 목적:** 네이버 금융 수급 크롤링이 실패하거나 빈 결과를 반환해도 전체 자동화가 중단되지 않도록 했습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `get_investor_data()`에서 수급 데이터가 없을 때 예외를 발생시키지 않고 `ticker`, `foreign_net`, `inst_net` 컬럼을 가진 빈 DataFrame을 반환하도록 변경했습니다.
  - 네이버 크롤링 실패 시 `[Warning]` 로그를 남기고 수급 금액을 0으로 대체해 거래대금 기반 수집/저장 흐름이 계속 진행되도록 했습니다.
  - 부분 수급 데이터가 들어온 경우에도 `foreign_net`, `inst_net` 컬럼이 항상 존재하도록 방어 로직을 추가했습니다.
---

## [2026-05-28 01:42] 크롤러 KST 기준 저장 및 데이터 품질 로그 추가
- **작업 목적:** GitHub Actions의 UTC 실행 환경에서도 수집 날짜와 세션명이 한국 시간 기준으로 저장되도록 하고, 앞으로 들어오는 데이터 품질을 Actions 로그에서 확인할 수 있게 했습니다.
- **영향을 받은 파일:** `crawler.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `ZoneInfo("Asia/Seoul")`을 사용해 `target_date`와 DB 저장 세션명을 KST 기준으로 계산하도록 변경했습니다.
  - `save_to_db()`에 카테고리별 저장 행 수, 빈 종목명 수, 가격 0건, 거래대금 0건을 출력하는 품질 로그를 추가했습니다.
  - 저장 대상이 0건이거나 주요 값이 비어 있는 경우 `[Warning]` 로그를 남기도록 했습니다.
---

## [2026-05-28 01:30] 세션 시간순 정렬 적용
- **작업 목적:** 자동 실행 후 텔레그램 브리핑과 웹 대시보드가 세션명을 문자열순으로 정렬해 최신/이전 세션을 잘못 판단할 수 있는 문제를 해결했습니다.
- **영향을 받은 파일:** `analyzer.py`, `app.py`, `ai_changelog.md`
- **주요 변경 사항:**
  - `StockAnalyzer._session_sort_key()`를 추가해 세션명 괄호 안의 `HH:MM` 값을 기준으로 최신 세션과 이전 세션을 판단하도록 변경했습니다.
  - Streamlit 대시보드에도 `session_sort_key()`를 추가해 사이드바 세션 선택과 직전 세션 비교가 실제 시간순으로 동작하도록 변경했습니다.
---

## [2026-05-28 01:21] GitHub Actions KIS API 환경변수 연결
- **작업 목적:** 자동 실행 환경에서 KIS API 인증값이 `main.py` 실행 단계로 전달되지 않아 크롤링이 실패할 수 있는 문제를 해결했습니다.
- **영향을 받은 파일:** `.github/workflows/main.yml`, `ai_changelog.md`
- **주요 변경 사항:**
  - GitHub Actions의 메인 자동화 실행 step `env` 블록에 `KIS_APP_KEY`, `KIS_APP_SECRET` secret 전달 설정을 추가했습니다.
  - 기존 텔레그램 secret 전달 및 실행 흐름은 그대로 유지했습니다.
---

## [2026-05-28 01:10] 로컬 실행 환경 복구
- **작업 목적:** 깨진 가상환경을 현재 설치된 Python 3.10 기준으로 복구하고, 프로젝트 실행에 필요한 패키지 import 및 분석 모듈 실행이 가능하도록 정비했습니다.
- **영향을 받은 파일:** `venv/`, `ai_changelog.md`
- **주요 변경 사항:**
  - `venv`의 Python 기준 경로를 현재 사용 가능한 Python 3.10 환경으로 갱신했습니다.
  - `requirements.txt` 기준 패키지 설치 상태를 확인하고, Python 3.11용 바이너리가 남아 있던 주요 패키지(`numpy`, `pandas`, `pyarrow`, `matplotlib` 등)를 Python 3.10용으로 강제 재설치했습니다.
  - 핵심 패키지 import, 주요 Python 파일 문법 검사, `StockAnalyzer.run_analysis()` 실행, Streamlit 버전 확인으로 로컬 실행 환경을 검증했습니다.
---
## [2026-06-27 02:41] 섹터 자금 그래프와 최근 섹터 흐름 가독성 개선
- **작업 목적:** 섹터별 자금 그래프에서 특정 대형 섹터가 다른 섹터를 눌러 보이지 않는 문제를 줄이고, 최근 섹터 흐름을 이번주 정규장 기준으로 일관되게 볼 수 있도록 개선했습니다.
- **영향을 받은 파일:** `app.py`, `requirements.txt`, `ai_changelog.md`
- **주요 변경 사항:**
  - `섹터별 자금`: 거래대금 막대그래프를 Altair 기반 압축 스케일 그래프로 변경해 반도체 메모리처럼 큰 섹터가 있어도 다른 섹터가 함께 보이도록 개선
  - `최근 섹터 흐름`: 이번주 월요일부터 선택 날짜까지의 `정규장(16:00)` 및 `VOLUME_TOP_60` 데이터만 사용하도록 필터링
  - `최근 섹터 흐름`: 매 시점마다 바뀌는 상위 섹터 대신 이번주 누적 거래대금 상위 5개 섹터를 고정해 흐름이 끊기지 않도록 변경
  - `requirements.txt`: 배포 환경에서 차트 렌더링이 안정적으로 동작하도록 `altair` 의존성 명시
---

## [2026-06-27 02:32] 시장강도 오전/수동 흐름 분석 추가
- **작업 목적:** 오전 장 흐름과 사용자가 버튼을 누른 시점 기준의 시장강도 흐름을 확인할 수 있도록 시장강도 분석 시간대와 실행 방식을 확장했습니다.
- **영향을 받은 파일:** `market_strength.py`, `main.py`, `app.py`, `.github/workflows/main.yml`, `stock_data.db`, `ai_changelog.md`
- **주요 변경 사항:**
  - `MarketStrengthAnalyzer`: 오전 흐름(09:15/09:30/09:45), 종가 흐름(14:30/15:00/15:20/15:30), 수동 흐름(기준시각/15분 전/30분 전)을 각각 저장할 수 있도록 분석 타입과 스냅샷 시간 목록을 분리
  - `market_strength_snapshots`: `analysis_type`, `analysis_label` 컬럼과 복합 기본키를 추가해 오전/종가/수동 분석이 서로 덮어쓰지 않도록 DB 구조 변경
  - `app.py`: 시장강도 탭에 `지금 기준 시장강도 분석 실행` 버튼과 흐름 선택 UI를 추가하고, 버튼 클릭 시 기준시각을 GitHub Actions에 전달
  - `.github/workflows/main.yml`: KST 09:50 오전 시장강도 자동 실행을 추가하고, 수동 실행 입력값을 `main.py`로 전달
---

## [2026-06-27 02:14] 외인/기관 순매수 종목별 조회로 전환
- **작업 목적:** 거래대금 표의 외국인/기관 순매수 값이 MTS와 다르게 보이는 문제를 줄이기 위해, 기존 순매수 랭킹 API 대신 종목별 투자자 순매수 API 값을 사용하도록 변경했습니다.
- **영향을 받은 파일:** `crawler.py`, `stock_data.db`, `ai_changelog.md`
- **주요 변경 사항:**
  - `StockCrawler.get_investor_data`: KIS `foreign-institution-total` 랭킹 조회 대신 `inquire-investor` 종목별 조회를 사용해 `frgn_ntby_tr_pbmn`, `orgn_ntby_tr_pbmn` 값을 수집
  - `StockCrawler.run`: 거래대금 상위로 수집된 종목 목록을 기준으로 각 종목의 외국인/기관 순매수 금액을 병합하도록 변경
  - `stock_data.db`: 최신 `20260626 정규장(16:00)` 데이터의 외국인/기관 순매수 값을 종목별 KIS 조회 값으로 갱신
---
